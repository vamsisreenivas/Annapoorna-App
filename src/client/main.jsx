import React from 'react';
import ReactDOM from 'react-dom/client';
import RestaurantPOS from './components/RestaurantPOS.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RestaurantPOS />
  </React.StrictMode>
);